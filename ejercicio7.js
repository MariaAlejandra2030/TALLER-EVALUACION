var nota = 120;

if (nota >=0 && nota<=20)
        { 
    console.log ("La calificación es A " + nota);
        }
   
   else if (nota >20 && nota<=40) 
        {
       console.log ("La calificación es B " + nota);
        }
   
   if  (nota >40 && nota<=60)     
        {
        console.log ("La calificación es C " + nota);
        }

   else if (nota >60 && nota<=80)
        {
        console.log ("La calificación es D " + nota);
        }
    if  (nota >80 && nota<=100)     
        {
            console.log ("La calificación es F " + nota);
        }
    else if (nota >100)
        {
            console.log ("La calificación es invalida");
        }